server {
    listen {{ web_listen_port }} default_server;
    listen [::]:{{ web_listen_port }} default_server;

    server_name {{ web_server_name }};
    root {{ web_document_root }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
